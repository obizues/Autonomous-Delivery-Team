[project]
name = "simple-auth-service"
version = "0.1.0"
description = "Seed auth repo for ai_software_factory repo-mode testing"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

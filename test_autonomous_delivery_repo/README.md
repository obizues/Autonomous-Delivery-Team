# test_autonomous_delivery_repo

A minimal Streamlit app to submit feature requests and view before/after results for each request.

## Usage

1. Install dependencies:
   ```
pip install -r requirements.txt
   ```
2. Run the app:
   ```
streamlit run app.py
   ```
3. Submit feature requests and view the before/after placeholders in the UI.
